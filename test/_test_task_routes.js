
const assert = require('assert');

describe('Task endpoints', () => {
  it('create: /todo/v1/task/add', (done) => {
    assert(1 + 1 === 2);
    done();
  });

  it('update: /todo/v1/task/add', (done) => {
    assert(1 + 1 === 2);
    done();
  });

  it('find by id: /todo/v1/task/add', (done) => {
    assert(1 + 1 === 2);
    done();
  });

  it('find all: /todo/v1/task/add', (done) => {
    assert(1 + 1 === 2);
    done();
  });

  it('delete: /todo/v1/task/add', (done) => {
    assert(1 + 1 === 2);
    done();
  });
});
